{% extends 'chore/base_dashboard.html.twig' %}

{% block title %}Event{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h1>Event</h1>
            </div>
            <div class="card-body">
                {% include 'app/event/_datasource.html.twig' %}
            </div>
            <div class="card-footer">
                <a class="btn btn-outline-secondary" href="{{ path('app_event_index') }}">Back to list</a>
                <a class="btn btn-outline-primary" href="{{ path('app_event_edit', {'id': event.id}) }}">Edit</a>
                {{ include('app/event/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
