{% extends 'chore/base_dashboard.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header">
                <h1>User</h1>
            </div>
            <div class="card-body">
                {% include 'chore/user/_datasource.html.twig' %}
            </div>
            <div class="card-footer">
                <a class="btn btn-outline-secondary" href="{{ path('app_user_index') }}">Back to list</a>
                <a class="btn btn-outline-primary" href="{{ path('app_user_edit', {'id': user.id}) }}">Edit</a>
                {{ include('chore/user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
